

set up `main.py` as a cron job every morning.
This means that new jobs are scraped every morning and the all_jobs.json gets updated,
and i am emailed every day.


have the website running constantly.

to do this, run the command:

```
flask --app backend run --host 0.0.0.0
```



